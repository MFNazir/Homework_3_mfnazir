<%= form_with(model: metalitem, local: true) do |form| %>
  <% if metalitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(metalitem.errors.count, "error") %> prohibited this metalitem from being saved:</h2>

      <ul>
        <% metalitem.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :unit_price %>
    <%= form.number_field :unit_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
